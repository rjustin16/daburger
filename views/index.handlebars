<h1>Eat da burger!</h1>

<h2>Burgers to eat:</h2>

<ul>
  {{#each burgers}}
  {{#unless devoured}}
  {{> burger devoured=false}}
  {{/unless}}
  {{/each}}
</ul>

<h2>Inside your belly:</h2>

<ul>
  {{#each burgers}}
  {{#if devoured}}
  {{> burger devoured=true}}
  {{/if}}
  {{/each}}
</ul>

<h2>Custom burger</h2>
<form class="form">
  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="burger_name">
    <button type="submit" class="btn btn-warning">Order</button>
  </div>
</form>